{
  "name": "heirlom",
  "private": true,
  "version": "0.1.0",
  "description": "HEIRLOM — a browser farm game where the genetics are the product.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w @heirlom/genetics && npm run build -w @heirlom/server && npm run build -w @heirlom/web",
    "test": "npm run test -w @heirlom/genetics && npm run test -w @heirlom/server",
    "test:genetics": "npm run test -w @heirlom/genetics",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "dev:server": "npm run dev -w @heirlom/server",
    "dev:web": "npm run dev -w @heirlom/web",
    "db:push": "npm run db:push -w @heirlom/server",
    "db:migrate": "npm run db:migrate -w @heirlom/server"
  },
  "engines": {
    "node": ">=20"
  }
}
